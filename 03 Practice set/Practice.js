// create a vairable of type string and try to add a number to it
let a = "Zee"
let b = 6
console.log(a + b)

// use typeof opreator to find the datatype od the string in last question
console.log(typeof (a+b))

// create a const object in js can you change and hold the numbar later
const a1={
    name:"Zee",
    section:1,
    salary:true
}
//a1="code"    ||   it will throw error b/c we use the const in it

// try to add new key in 3rd question
a1['friend']='code'
console.log(a1)